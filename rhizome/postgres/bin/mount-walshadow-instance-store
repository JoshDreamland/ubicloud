#!/usr/bin/env ruby

# frozen_string_literal: true

require_relative "../lib/walshadow_instance_store"

if ARGV.count != 1
  fail "Wrong number of arguments. Expected 1, Given #{ARGV.count}"
end

WalshadowInstanceStore.new(ARGV[0]).setup
